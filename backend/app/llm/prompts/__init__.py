"""Prompt template files for the LLM runtime.

Templates live here as plain files (``*.md``) rather than Python strings so
they can be reviewed and versioned like documents. Loaded via
:func:`app.llm.runtime.load_system_prompt`.
"""
